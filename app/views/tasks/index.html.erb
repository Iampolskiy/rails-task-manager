<h2>index.thml.erb</h2>
<h2>My Tasks:</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to(task.title, task_path(task.id)) %></li>
  <% end %>
</ul>

<h1><%= link_to('new task', tasks_new_path) %></h1>
